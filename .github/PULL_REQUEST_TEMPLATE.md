## Summary

What does this change do, and why? (Link the issue it resolves, if any.)

## Type of change

- [ ] Bug fix
- [ ] New feature (within an already-approved capability's scope)
- [ ] Documentation
- [ ] Architecture / governance (requires an ADR — see `docs/adr/README.md`)
- [ ] Other (describe):

## Evidence

- [ ] Tests added or updated for this change.
- [ ] `npx tsc --noEmit` passes (if TypeScript changed).
- [ ] `npm run lint` passes.
- [ ] `npm run test` (unit) passes.
- [ ] `npm run test:integration` passes, if this touches a service with real-Postgres integration coverage.
- [ ] Documentation updated, if this changes a documented behavior, API, or architectural boundary.

## Frozen / governed artifacts

- [ ] This PR does **not** modify any frozen baseline (`docs/execution/*_BASELINE_*.md`), certification report, or certification dataset.
- [ ] If this PR touches a Certification Standard, it is additive only, via a dedicated Amendment document (see `docs/certification/`).
- [ ] If this PR touches a Foundation/Reference Architecture or Governance Standard boundary, an ADR is linked: #

## Screenshots (if a UI change)

## Additional context
